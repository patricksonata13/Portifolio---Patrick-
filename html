<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Patrick Sonata - Portfólio</title>
    <style>
        /* Estilos gerais */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background-color: #f5f5f5;
            color: #333;
            line-height: 1.6;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        /* Cabeçalho */
        header {
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            color: white;
            text-align: center;
            padding: 60px 20px;
            margin-bottom: 40px;
        }
        
        header h1 {
            font-size: 3rem;
            margin-bottom: 10px;
        }
        
        header p {
            font-size: 1.2rem;
            opacity: 0.9;
        }
        
        /* Seções */
        section {
            margin-bottom: 50px;
            padding: 30px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }
        
        h2 {
            font-size: 2rem;
            margin-bottom: 25px;
            color: #2575fc;
            display: flex;
            align-items: center;
        }
        
        h2 span {
            margin-right: 10px;
        }
        
        /* Grid de imagens */
        .image-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
        }
        
        .image-card {
            background: #f9f9f9;
            border-radius: 8px;
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        
        .image-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
        }
        
        .image-placeholder {
            height: 200px;
            background: linear-gradient(45deg, #6a89cc, #4a69bd);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
        }
        
        .image-info {
            padding: 15px;
        }
        
        .image-info h3 {
            margin-bottom: 10px;
            color: #333;
        }
        
        /* Formulário de upload */
        .upload-section {
            text-align: center;
            padding: 40px 20px;
        }
        
        .upload-area {
            border: 2px dashed #6a89cc;
            border-radius: 8px;
            padding: 40px;
            margin: 30px auto;
            max-width: 600px;
            background: #f8f9ff;
            cursor: pointer;
            transition: background 0.3s;
        }
        
        .upload-area:hover {
            background: #eef2ff;
        }
        
        .upload-icon {
            font-size: 3rem;
            color: #6a89cc;
            margin-bottom: 15px;
        }
        
        .upload-button {
            background: #4a69bd;
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
            transition: background 0.3s;
            margin-top: 20px;
        }
        
        .upload-button:hover {
            background: #1e3799;
        }
        
        /* Rodapé */
        footer {
            text-align: center;
            padding: 30px;
            background: #333;
            color: white;
            margin-top: 50px;
        }
        
        /* Responsividade */
        @media (max-width: 768px) {
            header h1 {
                font-size: 2.2rem;
            }
            
            .image-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <h1>🎭 Patrick Sonata</h1>
            <p>Roteirista | Comediante | Criador de Histórias</p>
        </div>
    </header>
    
    <div class="container">
        <!-- Seção de Trajetória -->
        <section id="trajetoria">
            <h2><span>📖</span> Trajetória</h2>
            <div class="image-grid">
                <div class="image-card">
                    <div class="image-placeholder">Trajetória - Parte 1</div>
                    <div class="image-info">
                        <h3>Minha Jornada</h3>
                        <p>Os primeiros passos na minha carreira artística</p>
                    </div>
                </div>
                <div class="image-card">
                    <div class="image-placeholder">Trajetória - Parte 2</div>
                    <div class="image-info">
                        <h3>Evolução Profissional</h3>
                        <p>Desenvolvimento das habilidades e conquistas</p>
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Seção de TV e Streaming -->
        <section id="tv">
            <h2><span>📺</span> Roteiro para TV e Streaming</h2>
            <div class="image-grid">
                <div class="image-card">
                    <div class="image-placeholder">Pablo & Luisão</div>
                    <div class="image-info">
                        <h3>Pablo & Luisão</h3>
                        <p>Roteiro para programa de comédia</p>
                    </div>
                </div>
                <div class="image-card">
                    <div class="image-placeholder">Zorra</div>
                    <div class="image-info">
                        <h3>Zorra</h3>
                        <p>Participação em programa de humor</p>
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Seção de Espetáculos -->
        <section id="espetaculos">
            <h2><span>🎭</span> Espetáculos e Shows</h2>
            <div class="image-grid">
                <div class="image-card">
                    <div class="image-placeholder">Pelada - A Hora da Gaymada</div>
                    <div class="image-info">
                        <h3>Pelada</h3>
                        <p>Espetáculo de comédia stand-up</p>
                    </div>
                </div>
                <div class="image-card">
                    <div class="image-placeholder">Show Jamaica</div>
                    <div class="image-info">
                        <h3>Show Jamaica</h3>
                        <p>Performance de comédia</p>
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Seção de Documentários -->
        <section id="documentarios">
            <h2><span>🎬</span> Documentário</h2>
            <div class="image-grid">
                <div class="image-card">
                    <div class="image-placeholder">Cidade Correria</div>
                    <div class="image-info">
                        <h3>Cidade Correria</h3>
                        <p>Documentário sobre cultura urbana</p>
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Seção de Séries e Humor -->
        <section id="series">
            <h2><span>😂</span> Séries e Humor</h2>
            <div class="image-grid">
                <div class="image-card">
                    <div class="image-placeholder">Humor Negro</div>
                    <div class="image-info">
                        <h3>Humor Negro</h3>
                        <p>Série de comédia</p>
                    </div>
                </div>
                <div class="image-card">
                    <div class="image-placeholder">Originais da Comédia</div>
                    <div class="image-info">
                        <h3>Originais da Comédia</h3>
                        <p>Participação especial</p>
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Seção de Oficinas -->
        <section id="oficinas">
            <h2><span>📚</span> Oficinas e Aulas</h2>
            <div class="image-grid">
                <div class="image-card">
                    <div class="image-placeholder">Estude o Funk</div>
                    <div class="image-info">
                        <h3>Estude o Funk</h3>
                        <p>Oficina de criação e roteiro</p>
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Seção de Upload -->
        <section class="upload-section">
            <h2><span>⬆️</span> Adicionar Novas Imagens</h2>
            <p>Faça upload de novas imagens para seu portfólio</p>
            
            <div class="upload-area" id="dropArea">
                <div class="upload-icon">📁</div>
                <p>Arraste imagens para aqui ou</p>
                <input type="file" id="fileInput" accept="image/*" multiple style="display: none;">
                <button class="upload-button" onclick="document.getElementById('fileInput').click()">Selecionar Arquivos</button>
            </div>
            
            <div id="previewContainer" style="margin-top: 20px;"></div>
        </section>
    </div>
    
    <footer>
        <div class="container">
            <p>© 2025 Patrick Sonata - Todos os direitos reservados</p>
        </div>
    </footer>

    <script>
        // Configuração do upload de imagens
        const dropArea = document.getElementById('dropArea');
        const fileInput = document.getElementById('fileInput');
        const previewContainer = document.getElementById('previewContainer');
        
        // Prevenir comportamento padrão para drag and drop
        ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
            dropArea.addEventListener(eventName, preventDefaults, false);
        });
        
        function preventDefaults(e) {
            e.preventDefault();
            e.stopPropagation();
        }
        
        // Destacar área de drop quando um arquivo estiver sobre ela
        ['dragenter', 'dragover'].forEach(eventName => {
            dropArea.addEventListener(eventName, highlight, false);
        });
        
        ['dragleave', 'drop'].forEach(eventName => {
            dropArea.addEventListener(eventName, unhighlight, false);
        });
        
        function highlight() {
            dropArea.style.background = '#e6eeff';
        }
        
        function unhighlight() {
            dropArea.style.background = '#f8f9ff';
        }
        
        // Manipular arquivos soltos
        dropArea.addEventListener('drop', handleDrop, false);
        
        function handleDrop(e) {
            const dt = e.dataTransfer;
            const files = dt.files;
            handleFiles(files);
        }
        
        // Manipular seleção de arquivos via botão
        fileInput.addEventListener('change', function() {
            handleFiles(this.files);
        });
        
        function handleFiles(files) {
            previewContainer.innerHTML = '';
            
            if (files.length === 0) return;
            
            const info = document.createElement('p');
            info.textContent = `${files.length} imagem(ns) selecionada(s):`;
            info.style.marginBottom = '15px';
            info.style.fontWeight = 'bold';
            previewContainer.appendChild(info);
            
            for (let i = 0; i < files.length; i++) {
                const file = files[i];
                
                if (!file.type.match('image.*')) {
                    continue;
                }
                
                const reader = new FileReader();
                
                reader.onload = function(e) {
                    const preview = document.createElement('div');
                    preview.style.display = 'inline-block';
                    preview.style.margin = '10px';
                    preview.style.position = 'relative';
                    
                    const img = document.createElement('img');
                    img.src = e.target.result;
                    img.style.height = '100px';
                    img.style.borderRadius = '5px';
                    img.style.boxShadow = '0 2px 5px rgba(0,0,0,0.2)';
                    
                    const removeBtn = document.createElement('button');
                    removeBtn.textContent = '×';
                    removeBtn.style.position = 'absolute';
                    removeBtn.style.top = '-5px';
                    removeBtn.style.right = '-5px';
                    removeBtn.style.background = 'red';
                    removeBtn.style.color = 'white';
                    removeBtn.style.border = 'none';
                    removeBtn.style.borderRadius = '50%';
                    removeBtn.style.width = '20px';
                    removeBtn.style.height = '20px';
                    removeBtn.style.cursor = 'pointer';
                    removeBtn.onclick = function() {
                        preview.remove();
                    };
                    
                    preview.appendChild(img);
                    preview.appendChild(removeBtn);
                    previewContainer.appendChild(preview);
                }
                
                reader.readAsDataURL(file);
            }
            
            // Simular upload (em um caso real, aqui você faria uma requisição para o servidor)
            setTimeout(() => {
                alert('Imagens carregadas com sucesso! Em uma aplicação real, elas seriam enviadas para o servidor.');
            }, 1000);
        }
    </script>
</body>
</html>
